---

- name: Configure the server
  sudo: yes
  hosts:
    - tag_osp_ranks

  vars_files:
    - vars/elasticsearch.yml
    - vars/postgres.yml

  roles:
    - common
    - postgres
    - elasticsearch
    - nginx
    - nodejs
